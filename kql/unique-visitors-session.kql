// Distinct visitors estimated from session IDs for the selected range
let start={{timeStart}};
let end={{timeEnd}};
{{tableName}}
| where timestamp between (start..end)
| extend canonicalSessionId = {{sessionIdExpr}}
| summarize uniqueVisitors = dcountif(canonicalSessionId, isnotempty(canonicalSessionId))
