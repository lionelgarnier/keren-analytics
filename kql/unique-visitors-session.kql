let start={{timeStart}};
let end={{timeEnd}};
{{tableName}}
| where timestamp between (start..end)
| extend canonicalSessionId = {{sessionIdExpr}}
| summarize uniqueVisitors = dcount(canonicalSessionId)
