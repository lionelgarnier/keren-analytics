// Top custom event names by count for the Technical events view
let start={{timeStart}};
let end={{timeEnd}};
customEvents
| where timestamp between (start..end)
| summarize count = count() by name
| top 50 by count desc
